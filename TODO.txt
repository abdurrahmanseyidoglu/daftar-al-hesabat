1- Create RecordsDiffsDialog, its job is to show that the local data and the cloud data are not the same and ask the user which version he wants to keep 
2- After that make sure that each record change on records should make a sync call to drive, essentially replacing the old content with the new one (This can make the requests heavy with time, but now that much since its a json file)
3- finish working on the sync logic