- i18 needs to be worked on things like Directions (some MUI component needs explicit rtl attributes)
- Enhance the add new modal style
- move filtering / sorting / calculations function from Zustand to utility folder fpr best practice
- Add the ability to import and export to different formats
    import: json,csv
    export: json,csv,pdf 
- Add the ability for syncing

- Work in person page to have records by currency not everything mixed up
- Exporting as CSV: if it's on the global records, 
                    the name should have the currency, so records_usd.csv or records_tl.csv 
                    i want to remove the direction column and replace it with this:
                    of its on then the money is on him which means he should pay me so the amount stays like it is
                    if it's to then its to him which means i need to pay him so it should be in minus,
                    also i want to add a last line which has the total
                    whether i should pay him x amount of money in x currency or he should pay me x amount of money with x currency
                    you can add the currency as an argument i wall pass it to you like "usd" , "tl" its going to be a string, you display it in uppercase though
                    you need to filter the records by currency before doing the math, dont worry i have this function, so the records will be filtered by currency
                    